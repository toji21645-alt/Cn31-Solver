[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.deploy]
cmds = ["python necap.py"]

[build]
env = {
  "PYTHONUNBUFFERED": "1",
  "PYTHONOPTIMIZE": "2",
  "PYTHONDONTWRITEBYTECODE": "1"
}